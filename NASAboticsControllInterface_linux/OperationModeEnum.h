#ifndef OPERATINGMODEENUM_H
#define OPERATINGMODEENUM_H

//NASAbot operating mode enum (Yes this is bad codding including this in the same .h file...)
enum OperationMode 
{
	AUTONOMOUS,
	SEMI_AUTONOMOUS,
	RC
};

#endif // OPERATINGMODEENUM_H
